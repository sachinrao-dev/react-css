import { makeStyles } from "@mui/styles";

const useStyle = makeStyles({
  border: {
    width: "100%",
    border: "1px solid",
    opacity: "0.1",
    marginTop: "20px",
  },
});

export default useStyle;
